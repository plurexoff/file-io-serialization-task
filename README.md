# Задание №3: Работа с файлами и потоками ввода-вывода

Репозиторий для практического задания по работе с файлами, сериализацией и десериализацией данных на Python.

## 📋 Цель задания

Получение навыков работы с файлами и сериализацией данных:
- Изучение методов открытия файлов и операций чтения/записи
- Создание приложения для работы с двоичными данными
- Применение сериализации собственных объектов с помощью `pickle`

## 📁 Структура проекта

```
file-io-serialization-task/
├── README.md
├── models.py              # Определение классов данных
├── file_operations.py     # Операции с файлами (чтение/запись)
├── serialization.py       # Сериализация и десериализация
├── main.py               # Основное приложение
├── examples/
│   ├── binary_data.py    # Работа с двоичными данными
│   └── json_alternative.py  # Альтернатива с JSON
├── tests/
│   ├── test_file_ops.py
│   ├── test_serialization.py
│   └── test_models.py
└── data/                 # Директория для хранения файлов
    ├── .gitkeep
    ├── objects.pkl       # Сериализованные объекты
    └── binary.bin        # Двоичные данные
```

## 🚀 Как использовать

### 1. Клонирование репозитория

```bash
git clone https://github.com/plurexoff/file-io-serialization-task.git
cd file-io-serialization-task
```

### 2. Установка зависимостей

```bash
# Основные модули входят в стандартную библиотеку Python
# Для тестирования (опционально):
pip install pytest
```

### 3. Запуск основного приложения

```bash
python main.py
```

### 4. Запуск примеров

```bash
# Пример с двоичными данными
python examples/binary_data.py

# Пример с JSON
python examples/json_alternative.py
```

### 5. Запуск тестов

```bash
pytest tests/
```

## 📚 Содержание модулей

### `models.py`
Определение классов данных:
- `User` - класс пользователя
- `Product` - класс продукта
- `Order` - класс заказа

```python
class User:
    def __init__(self, user_id, name, email):
        self.user_id = user_id
        self.name = name
        self.email = email
```

### `file_operations.py`
Функции для работы с файлами:
- `write_binary(filename, data)` - запись двоичных данных
- `read_binary(filename)` - чтение двоичных данных
- `write_text(filename, content)` - запись текста
- `read_text(filename)` - чтение текста

### `serialization.py`
Функции сериализации:
- `serialize_object(obj, filename)` - сохранение объекта в pickle
- `deserialize_object(filename)` - загрузка объекта из pickle
- `serialize_to_bytes(obj)` - сериализация в байты
- `deserialize_from_bytes(data)` - десериализация из байтов

### `main.py`
Основное приложение с интерактивным меню:
- Создание и сохранение объектов
- Загрузка объектов из файла
- Просмотр сохраненных данных
- Работа с двоичными данными

## 💡 Ключевые концепции

### Сериализация с pickle

```python
import pickle

# Сохранение объекта
with open('data.pkl', 'wb') as f:
    pickle.dump(obj, f)

# Загрузка объекта
with open('data.pkl', 'rb') as f:
    obj = pickle.load(f)
```

### Работа с двоичными файлами

```python
# Запись двоичных данных
with open('file.bin', 'wb') as f:
    f.write(b'\x00\x01\x02\x03')

# Чтение двоичных данных
with open('file.bin', 'rb') as f:
    data = f.read()
```

### Context Manager (with statement)

```python
# Файл автоматически закроется после выхода из блока
with open('file.txt', 'r') as f:
    content = f.read()
```

## ⚠️ Важные замечания

1. **Безопасность pickle**: Не десериализуйте данные из ненадежных источников!
2. **Совместимость версий**: Pickle может быть несовместим между версиями Python
3. **Альтернативы**: Для текстовых данных лучше использовать JSON, CSV или YAML
4. **Обработка ошибок**: Всегда обрабатывайте исключения при работе с файлами

## 🧪 Примеры тестирования

```bash
# Запуск всех тестов
pytest tests/ -v

# Запуск конкретного теста
pytest tests/test_serialization.py::test_serialize_user -v

# С покрытием кода
pytest tests/ --cov=. --cov-report=html
```

## 📖 Дополнительные ресурсы

- [Python pickle documentation](https://docs.python.org/3/library/pickle.html)
- [Python file I/O documentation](https://docs.python.org/3/tutorial/inputoutput.html#reading-and-writing-files)
- [PEP 307 - Extensions to the pickle protocol](https://www.python.org/dev/peps/pep-0307/)

## 👤 Автор

plurexoff

## 📄 Лицензия

MIT